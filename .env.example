# -------------------------------------------------------------------
# Example environment variables for running Airflow + dbt + Snowflake
# -------------------------------------------------------------------

# Snowflake credentials (replace with your own values in .env)
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password
SNOWFLAKE_ACCOUNT=your_account.region   # e.g. abc12345.us-east-1
SNOWFLAKE_DATABASE=dbt_db
SNOWFLAKE_SCHEMA=dbt_schema
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_ROLE=SYSADMIN

# Airflow UID (required by Astro for local dev on Linux/macOS)
AIRFLOW_UID=50000
